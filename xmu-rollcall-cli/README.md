# XMU Rollcall CLI

CLI tool for monitoring and handling rollcalls on Xiamen University Tronclass.

This file exists to satisfy packaging metadata (`setup.py` / `pyproject.toml`)
when installing this subproject independently.
